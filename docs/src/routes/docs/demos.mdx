import { FirstAnimation } from "~/components/demos/first-animation";
import { EnterAnimation } from "~/components/demos/enter-animation";
import { ExitAnimation } from "~/components/demos/exit-animation";
import { HoverAndTapAnimation } from "~/components/demos/hover-and-tap-animation";
import { LayoutAnimation } from "~/components/demos/layout-animation";
import { LayoutBorderRadius } from "~/components/demos/layout-border-radius";
import { LayoutPositionAnimation } from "~/components/demos/layout-position-animation";
import { LayoutUnderlineAnimation } from "~/components/demos/layout-underline-animation";
import { ScrollAnimation } from "~/components/demos/scroll-animation";
import { AnimatingLayoutBar } from "~/components/demos/animating-layout-bar";
import { ExpandableCard } from "~/components/demos/expandable-card";
import { FilterableGrid } from "~/components/demos/filterable-grid";

# Demos

Interactive examples demonstrating motion-solid's capabilities.

<div class="w-full flex justify-center items-center gap-y-6 flex-col my-12">

## First Animation

Basic enter animation with scale and opacity.

<FirstAnimation />

## Enter Animation

Smooth fade-in with position offset.

<EnterAnimation />

## Hover & Tap

Interactive button with scale feedback on hover and tap.

<HoverAndTapAnimation />

## Scroll Animation

Elements animate as they enter the viewport using `whileInView`.

<ScrollAnimation />

## Layout Animation

Position and size changes animate smoothly.

<LayoutAnimation />

## Layout Border Radius

Layout animation with border-radius morphing.

<LayoutBorderRadius />

## Layout Position

Shared element position transitions.

<LayoutPositionAnimation />

## Layout Underline

Shared `layoutId` for tab indicator underline.

<LayoutUnderlineAnimation />

## Animating Layout Bar

Layout animation demonstrating bar resizing.

<AnimatingLayoutBar />

## Expandable Card

A classic shared \`layoutId\` demo where tapping a card expands it into a modal. Notice how the text and background seamlessly morph between states.

<ExpandableCard />

## Filterable Grid

Demonstrates grid reflow with \`layout\` combined with enter and exit animations using \`AnimatePresence\`.

<FilterableGrid />

## Exit Animation

Exit animation when element is removed.

<ExitAnimation />

</div>
