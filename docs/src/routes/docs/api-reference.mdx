# API Reference

High-level docs live in the topic pages. This page is the short export list.

## Components

- `motion`
- `AnimatePresence`
- `LayoutGroup`
- `MotionConfig`

## Hooks

- `usePresence`
- `useIsPresent`
- `usePresenceData`
- `useInstantLayoutTransition`
- `useResetProjection`
- `useMotionConfig`
- `useReducedMotion`

## Utilities

- `createDragControls`
- `stagger`
- `isStaggerFunction`

## Types

- `MotionProps`
- `AnimatePresenceProps`
- `MotionConfigProps`
- `Variant`
- `Variants`
- `Transition`

## Notes

- `AnimatePresence` defaults to `mode="popLayout"` in `motion-solid`.
- Public transform and style keys are kebab-case first.
- Exit/layout handoff retains the DOM node, but Solid still disposes the exiting subtree owner.
